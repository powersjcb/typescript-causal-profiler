{
  "name": "typescript-causal-profiler",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clear": "rm -rf dist",
    "compile": "tsc --build",
    "test": "npm run compile && cd __example__ && node ../dist/src/index.js && node ./dist/testFile.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ttypescript": "^1.5.12",
    "typescript": "^4.1.2"
  },
  "devDependencies": {
    "ts-node": "^9.1.0"
  }
}
